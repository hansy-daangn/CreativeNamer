{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability: core APIs (events, window) + the native file dialog.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
